roles:
  stegtalk/ci/build:
    description: "StegTalk build pipeline (LLM-facing)."
    ttl_minutes: 30
    permissions:
      kv:read:
        - llm/provider_api_key
        - llm/rate_limit_bucket
