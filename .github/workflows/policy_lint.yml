name: Policy Lint (OPA/Rego)
on: { workflow_dispatch: {} }
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lint (stub)
        run: echo "No Rego policies bundled; lint pass."
