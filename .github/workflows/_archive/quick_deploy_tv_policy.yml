name: Quick Deploy TV Policy (optional)

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This is a placeholder you can replace with your policy import workflow."
