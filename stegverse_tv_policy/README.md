# Token Vault Config (Policy Repo)

Private repository for Token Vault (TV) roles, issuers, and rotation policy used by FREE-DOM and related projects.

Files:
- roles.yml    — role definitions and allowed keys
- issuers.yml  — trusted identity providers (e.g., GitHub OIDC) and constraints
- rotation_policy.md — rotation cadence and procedures
